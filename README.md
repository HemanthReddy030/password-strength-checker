# password-strength-checker
A Python-based CLI tool that evaluates password strength in real time using the keyboard module for secure masked input. Analyses passwords based on length, uppercase, lowercase, numbers &amp; special characters — classifying them as Weak, Moderate, or Strong with actionable feedback.
